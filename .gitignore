# Personal config — never commit (contains your real email addresses)
config.yaml
.ghostwriter/

# Internal dev note — real account emails, cron job IDs, Telegram setup.
# Kept local, never published.
HANDOVER.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# macOS
.DS_Store

# Runtime / scratch
*.tmp
*.processing.json
